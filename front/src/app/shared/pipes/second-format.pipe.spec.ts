import { SecondFormatPipe } from './second-format.pipe';

describe('SecondFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
